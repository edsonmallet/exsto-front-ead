/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'localhost', 'youtube.com'],
  },

  reactStrictMode: true,
}

module.exports = nextConfig
